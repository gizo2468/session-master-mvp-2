Update the `DialogTitle` in three modals to use the app's gold accent color (`text-primary`), matching other section headings.

## Files to change

- **`src/components/poker/BBStackUpdateModal.tsx`** — add `text-primary` to the "BB / Stack Update" `DialogTitle`.
- **`src/components/poker/HandTableSelectionModal.tsx`** — add `text-primary` to the "Select Table for Hand" `DialogTitle`.
- **`src/components/poker/BreakTimeModal.tsx`** — add `text-primary` to the "Take a Break" `DialogTitle`.

Only the title text color changes. Font size, weight, alignment, spacing, wording, close icon, body content, fields, buttons, borders, and background stay exactly as they are.