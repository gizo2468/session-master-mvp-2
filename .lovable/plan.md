## Update Advanced Options Tooltip Title

In `src/components/onboarding/tourSteps.ts`, update the step with selector `[data-tour="advanced-checkboxes"]`:

- Change `title` from `'Session Settings'` to `'Advanced Options (Optional)'`.
- Leave `body`, `compact: true`, `interactive`, `route`, and `prepare` unchanged so the description, padding, button row, and spotlight over the three checkboxes remain identical.

No other files or steps are modified.